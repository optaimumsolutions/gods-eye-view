// The hosted build's stand-in for data/bhoteKoshiFloodPath.js
// (server/hosting/licencePolicy.js). The GeoPera centreline is CC BY-NC 4.0,
// so the hosted bundle ships no vertices; the shot-path builder and the
// locator already treat an empty route as "no path".
export const BHOTE_KOSHI_FLOOD_PATH = Object.freeze([]);
