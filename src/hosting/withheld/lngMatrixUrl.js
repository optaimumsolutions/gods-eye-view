// The hosted build's stand-in for layers/lng/matrixUrl.js
// (server/hosting/licencePolicy.js). No GIIGNL matrix ships: the LNG source
// reads no matrix and the snapshot draws only the US cargo flows.
export const LNG_MATRIX_URL = null;
